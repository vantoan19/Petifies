import 'package:flutter_riverpod/flutter_riverpod.dart';

// Provide context to know whether it's a post or comment
final isPostContextProvider =
    Provider<bool>((ref) => throw UnimplementedError());
